import {Easing} from 'react-native';

export const DAY_SIZE = 30;
export const STATES = {
  HIDDEN: 'HIDDEN',
  ANIMATING: 'ANIMATING',
  SHOWN: 'SHOWN',
};
export const EASING = Easing.bezier(0.4, 0, 0.2, 1);
export const SCREEN_INDENT = 8;
